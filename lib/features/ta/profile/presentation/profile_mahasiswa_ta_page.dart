import 'package:flutter/material.dart';

class ProfileMahasiswaTAPage extends StatelessWidget {
  static const String routeName = '/profile-mahasiswa-ta';
  const ProfileMahasiswaTAPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Profile Mahasiswa TA Page'))
    );
  }
}
